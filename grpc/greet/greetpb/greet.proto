syntax = "proto3";
package greeting;

option go_package = "github.com/hwipl/snippets-go/grpc/greet/greetpb";

service Greeter {
  rpc Greet (Greeting) returns (Greeting) {}
}

message Greeting {
  string from_name = 1;
  string to_name = 2;
  string text = 3;
}
